#include "main.h"
#include <string.h>

/**
 * _strlen - Function to get the length of the string
 * @s: string pointer to pass to this string
 * Return: Returns length of string
 */

int _strlen(char *s)
{
	return (strlen(s));
}

